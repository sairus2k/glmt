stages:
  - build

build:
  stage: build
  image: golang:1.26
  script:
    - go build -o glmt ./cmd/glmt
  artifacts:
    paths:
      - glmt
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
